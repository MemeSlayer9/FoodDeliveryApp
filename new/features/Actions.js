import {ADDITION, ADD_ITEM, REMOVE_ITEM, SUBTRACTION} from './ActionTypes';
 export const addition = data => ({
    type: ADDITION,
 });

export const substraction = index => ({
    type: SUBTRACTION,
 });